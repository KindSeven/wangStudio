// import { create } from "zustand";
// interface BearState {
//   bears: number;
//   increase: (by: number) => void;
// }
// const useStore = create<BearState>((set) => ({
//   bears: 0, //初始值,用来存储熊的数量
//   increasePopulation: () => set((state) => ({ bears: state.bears + 1 })), //增加熊的数量
//   removeAllBears: () => set({ bears: 0 }), //清空熊的数量
// }));
// export default useStore;
